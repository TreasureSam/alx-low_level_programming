#include "main.h"
#include <stdio.h>

/* *
 * _isdigit - checks if a character is digit
 *  * @c: carrier variable
 *   * Return: 1 if true, 0 if false
 *   
 * */
int _isdigit(int c)
{
 c = '0';
 printf("%c: %d\n", c=0, _isdigit(c));
 c = '9';
 printf("%c: %d\n", c=9, _isdigit(c));
 return (0);
}
